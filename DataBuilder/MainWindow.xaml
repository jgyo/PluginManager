<Window x:Class="DataBuilder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataBuilder"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Label Content="Zip Files Folder" />
            <DockPanel Grid.Row="1" Grid.Column="0">
                <Button x:Name="Scan" Content="Scan" Padding="4 1" DockPanel.Dock="Right" Click="Scan_Click" />
                <Button x:Name="Browse" Content="Browse" Padding="4 1" DockPanel.Dock="Right" Click="Browse_Click" />
                <TextBlock x:Name="Folder" Text="folder" />
            </DockPanel>
            <DataGrid x:Name="collection" Grid.Row="2" Grid.Column="0" />
            <DockPanel Grid.Row="3" Grid.Column="0">
                <Button x:Name="Save" Content="Save" DockPanel.Dock="Right" Padding="4 1" Click="Save_Click" />
                <Label />
            </DockPanel>
        </Grid>
    </Grid>
</Window>